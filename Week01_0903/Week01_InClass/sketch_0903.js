function setup(){
    createCanvas(w, h, [renderer])
    background(100,0,25) 
    // these are top functions that runs over and over again

}


function draw(){
    background(color, [a])
    circle(mouseX, mouseY, 70) //注意顺序 会影响visual effect
    noStroke()

    console.log()

}